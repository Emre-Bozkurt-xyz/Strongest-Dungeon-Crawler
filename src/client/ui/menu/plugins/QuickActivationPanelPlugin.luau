--!strict

local plugin = {
	id = "QuickActivationPanelPlugin",
	tier = "context",
}

function plugin.mount(_host)
end

function plugin.onStateChanged(_prev, _next)
end

function plugin.onData(_event)
end

function plugin.unmount()
end

return plugin